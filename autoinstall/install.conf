# OpenRiot install.conf - Final tuned for interactive install

# Keyboard
Choose your keyboard layout ('?' or 'L' for list) [default] = default

# Hostname
System hostname? (short form, e.g. 'foo') = openriot-desktop

# Network - skip
Network interface to configure? (name, lladdr, '?', or 'done') = done

# Root password
Password for root account? (will not echo) = ask
Password for root account? (again) = ask

# SSH
Allow root ssh login? (yes, no, prohibit-password) [no] = no

# Timezone
What timezone are you in ('?' for list) [US/Pacific] = US/Pacific

# X11
Do you expect to run the X Window System? [yes] = no

# Disk partitioning
Which disk is the root disk? ('?' for details) [sd0] = sd0
Encrypt the root disk with a (p)assphrase or (k)eydisk? [no] = no
Use (W)hole disk MBR, whole disk (G)PT or (E)dit? [whole] = whole
Use (A)uto layout, (E)dit auto layout, or create (C)ustom layout? [a] = c

a	2G
b	1G
d	*

w
q

# Sets location
Location of sets? (cd0 cd1 disk http nfs or 'done') [cd0] = cd0
Pathname to the sets? (or 'done') [7.9/amd64] = 7.9/amd64

# Force select site79.tgz
Is the site79.tgz set part of the above list? = yes
Set name(s)? (or 'abort' or 'done') [done] = site79.tgz
Set name(s)? (or 'abort' or 'done') [done] = done

# Verification bypass - exact phrasing from your screenshot
Directory does not contain SHA256.sig. Continue without verification? = yes
Checksum test for site79.tgz failed. Continue anyway? = yes
Unverified sets: site79.tgz. Continue without verification? = yes
Continue without verification = yes

# Skip firmware
Skip firmware update = yes
