# Packages and modules to exclude from installer image
# These are NOT downloaded for image builds (make img)
# Normal installs via setup.sh still include everything

exclude:
  - go

modules:
  - desktop.games
