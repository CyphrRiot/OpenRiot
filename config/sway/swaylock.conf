# ArchRiot Swaylock Configuration
# Uses swaylock-wrapper.py (PIL) to generate /tmp/swaylock-bg.png with crypto, time, date, etc.
# swaylock displays the image as background.
image = /tmp/swaylock-bg.png
scaling = fill
hide_cursor = true
ignore_empty_input = false
show_dots = true
